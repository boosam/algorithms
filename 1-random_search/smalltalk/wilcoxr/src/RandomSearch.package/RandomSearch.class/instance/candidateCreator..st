accessing
candidateCreator: anObject
"A block whose signature should look like:

[  inArray | ]

This block MUST return an alternative array of the same length as the original provided array
"

	candidateCreator := anObject